apiVersion: v1
kind: Service
metadata:
  name: db-service
  namespace: room-booking
spec:
  selector:
    app: room-booking-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
